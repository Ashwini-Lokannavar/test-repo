name: Call a reusable workflow

on:
  push:
    branches:
      - main

jobs:
  call-workflow:
    uses: Travelport-Enterprise/devops-rest-apis-postman/blob/main/.github/workflows/postamantest.yml@v3

  
